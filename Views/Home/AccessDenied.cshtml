@{
    ViewBag.Title = "Acceso denegado";
}

<section class="error-page">
    <div class="error-card">
        <h1>Acceso denegado</h1>
        <p class="text-muted">No tenés permisos para realizar esta acción.</p>
        @Html.ActionLink("Volver al inicio", "Index", "Home", null, new { @class = "btn btn-danger mt-3" })
    </div>
</section>
