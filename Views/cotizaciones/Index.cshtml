@model IEnumerable<RedFlix.cotizaciones>

@{
    ViewBag.Title = "Index";
}

@{ ViewBag.ModuleTitle = "Cotizaciones"; ViewBag.ModuleSubtitle = "Historial de tipos de cambio"; ViewBag.CreateText = "Nueva cotizacion"; }

@Html.Partial("_ModuleHeader")

<div class="module-card module-card-table">
    <div class="table-responsive">
<table class="table module-table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Editar", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Detalle", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Eliminar", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
    </div>
</div>

